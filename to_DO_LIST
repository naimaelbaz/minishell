

git pull
git merge branch name


[ls -la] => split args;

// cd without args
if ()
// TODO search HOME
	if (chdir(expand->value) == -1)
	{
	}
		//ERROR
// elif
// TODO check if args == - or ~
// change directory chdir
